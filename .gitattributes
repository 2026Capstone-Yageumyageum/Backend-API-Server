# 저장소에는 항상 LF로 저장하고, 체크아웃 시 OS 기본을 따른다.
# 팀원마다 core.autocrlf 설정이 달라 줄바꿈만 바뀐 diff가 생기는 것을 막는다.
* text=auto eol=lf

# 실행 스크립트는 반드시 LF여야 한다.
gradlew text eol=lf
*.sh text eol=lf

# Windows 배치 파일은 CRLF여야 한다.
*.bat text eol=crlf

# 바이너리는 변환하지 않는다.
*.jar binary
*.png binary
*.jpg binary
*.mp4 binary
